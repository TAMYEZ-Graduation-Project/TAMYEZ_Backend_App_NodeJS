import DatabaseRepository from "./database.repository.js";
class FeedbackRepository extends DatabaseRepository {
    constructor(model) {
        super(model);
    }
}
export default FeedbackRepository;
